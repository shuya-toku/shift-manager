// Supabase connection config — fill in after creating your project.
// Leave as null to run without history (everything else still works).
//
// 1. https://supabase.com → New project
// 2. Settings → API → copy "Project URL" and "anon public" key
// 3. Run the SQL in SUPABASE-SETUP.md to create the shift_records table
// 4. Set ANTHROPIC_API_KEY in Vercel environment variables

window.SUPABASE_URL      = null;  // e.g. 'https://abcxyz.supabase.co'
window.SUPABASE_ANON_KEY = null;  // anon/public key (safe to expose)
