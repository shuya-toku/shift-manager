# TSK-103 大枠設計 v0（2026-07-04・Fable作成／設計のみ・実装保留）

> 週次「時間帯別稼働 実人数vs想定人数 → 翌週理論値・差分の可視化 ＆ OP改善ループ」（上長報告用）。
> 大枠⑤ SQAシフト人員最適化 / プロジェクト SCOPE。Notion管理チケット＝TSK-103。
> **このドキュメントは大枠設計。§8 の確認事項を確定してから v1 ドラフトへ。実装は承認後。**

## 1. アウトプット像（週次1枚レポート）
「先週どこで取りこぼし、翌週どこに人を足す/減らすか」を1枚で意思決定できることがゴール。
- ヘッダーKPI：週次ライブMiss率（WoW±）／夜間Miss率／人員不足コマ数（n/35）／前週打ち手の効果（○△×）
- 中央：時間帯×曜日ヒートマップ（差分＝実−想定、不足=赤／過剰=青／適正=白、Missed発生コマに⚠）
- 右：翌週の理論値 vs 現シフト予定 → 差分 → 手当て必要コマの抽出リスト（上位3〜5件）
- 下：夜間アラート（🔴無人Miss/🟡有人Miss明細）＋アクション台帳（前週OP変更→今週結果）
- 上長がこの1枚で判断：①翌週シフトの増減承認 ②夜間カバー方針（増員/AI委譲/許容）③OP変更の継続・撤回

## 2. 指標定義
粒度＝時間帯バンド×曜日：`04-07 / 07-12 / 12-16 / 16-21 / 21-02`（21-02は翌02時まで。02-04はバンド外→§8）。

**① 実人数（Actual Staffed）**：当該バンドの在席オペ数（SCOPEシフト、DE除く）。v1は「シフト予定＝実人数」で開始（欠勤反映の実績は§8）。

**② 想定人数（Required）— 2案併記**

| 案 | 算出 | 長所 | 短所 |
|---|---|---|---|
| A案(推奨・v1) 需要÷キャパ | ⌈demand_vol ÷ 1人あたり処理キャパ⌉。キャパはSCOPE自動推定をバンド別固定値化 | 既存資産で即算出・説明が単純 | 待ち時間/同時着信の偏りを無視 |
| B案(v2候補) Erlang C | 着信率・AHT・目標応答水準から必要席数 | 標準・夜間の少数着信に強い | AHT/目標SL整備が必要 |

推奨：v1はA案で開始→キャパ妥当性を上長レビュー→不足ならB案。乖離が大きいバンド（夜間低ボリューム帯）は注記。

**③ 翌週理論値**：(1)需要予測 →(2)②の式で人数化。需要予測は推奨＝直近4週の同「曜日×バンド」demand_volを 4:3:2:1 で加重移動平均。祝日/繁忙は手動補正欄（Shuyaが週初入力）。

**④ 差分（2種を区別）**

| 差分 | 式 | 使い道 |
|---|---|---|
| 当週ギャップ(振り返り) | 実人数−想定人数 | 先週どこが薄かったか。Missed実績と答え合わせ |
| 翌週ギャップ(先回り) | 翌週シフト予定−翌週理論値 | 翌週どのコマに手当てが要るか（上長承認対象） |

## 3. 夜間カバー欠落の検知
「取れない」＝`missed/demand_vol`（5月実績：ライブMiss率≈8.8%、ワースト15/22/16時）。2段階で検知：

| レベル | 条件 | 意味 | 対応 |
|---|---|---|---|
| 🔴無人Miss(構造) | missed>0 かつ 実人数=0 | 人がいない時間に着信 | シフト構造変更/AI・留守番委譲 |
| 🟡有人Miss(能力/運用) | missed>0 かつ 実人数≥1 かつ Miss率>閾値 | いるが捌けていない | 増員・平準化・処理フロー改善 |

夜間＝21-02＋02-04（hourlyで補完監視）。閾値(仮)＝夜間週次Miss率>5% or 週間missed≥N件。夜間独自目標を持つかは§8。

## 4. データソース
**足りる**：`v_staffing_volume_bands`（contact_date/band_id/total_vol/demand_vol/missed/mail_vol）・`_hourly`・`_missed_detail`（KPI Supabase `teovwpuubkefbvuuxzvo`）＋SCOPE在席。
**足りない**：シフト実績(欠勤反映)／在席のSupabase化（→突合ビュー `v_staffing_gap_weekly` 新設・実装保留）／キャパ確定値／祝日カレンダー。
粒度：レポート＝バンド×曜日、ドリル＝時間×日。拠点/チャネル分解はv2。

## 5. 週次レポート構成
①ヘッダーKPI4指標 ②時間帯×曜日マトリクス（当週ギャップ・発散配色・⚠）③翌週プラン表＋手当てリスト（推奨アクション1行）④夜間アラート明細（🔴/🟡・ゼロなら「夜間クリア」明示）⑤アクション台帳。媒体：v1は1枚資料を手動生成、v2でSCOPE週次タブ。

## 6. OP改善ループ（PDCA）
月曜朝レポート生成 → Shuya手当て仮説記入(15分) → 上長定例でOP変更決定（Notion「🗓 MTG・決定事項ログ」＋Tasks化・大枠⑤配下）→ 週内にシフト反映 → 翌週レポートで打ち手効果を自動突合（○継続/△様子見/×撤回）。打ち手にID付与（対象コマ・仮説・期待効果・結果）で蒸し返し防止。

## 7. 実装フェーズ

| Phase | 内容 | 完了条件 | 状態 |
|---|---|---|---|
| 0.設計(本書) | 指標/レポート像/ループの合意 | 上長レビュー通過・§8確定 | ✅ §8確定(2026-07-04) |
| 1.v1ドラフト | 直近1週を半手動作成(A案・加重平均) | 実データ入り1枚 | ✅ 実装済み(2026-07-04)。SCOPE内新タブ「📅 週次稼働レポート」(`weekly-staffing.js`)。Playwrightでヘッドレス動作確認済み |
| 2.上長レビュー | 定例で提示→閾値/キャパ/見せ方調整 | 「この形式で毎週見る」合意 | 未着手 |
| 3.定例化 | 在席Supabase化＋突合ビュー＋週次自動生成＋台帳運用 | 3週連続で自動運用 | 未着手 |

### v1の既知の制約（Phase2レビュー時に共有）
- 対象週はSupabase側の最新データ日から自動算出（「直近の完全なMon-Sun週」）。本番Shuyaのブラウザではローカル在席データ(シフト予定)が実際にその週まで入力されているため実数値が出るが、シフト未入力の週は在席0扱いになる。
- キャパ推定は直近9週(63日)の「完全な日」×バンドのdemand/staffed中央値。サンプルnが小さい/0のバンドは推定値が出ない（レポート内「キャパ推定値」表のnで確認可）。
- アクション台帳・祝日補正メモはブラウザのlocalStorage保存（この端末のみ）。Phase3でSupabase/Firestore永続化＆複数端末共有に対応予定。

## 8. 🚩 Shuya確認事項（v1ゲート）— 2026-07-04 確定

| # | 論点 | 確定値 |
|---|---|---|
| 1 | キャパ値(1人処理数)をバンド別固定でよいか | **自動推定をそのまま採用**。volume.jsの既存ロジック（完全な日のdemand/staffed中央値、バンド別）を踏襲し、v1ドラフトに実際の推定値を表示してレビューする |
| 2 | 想定人数はA案で開始でよいか（B案移行条件） | **A案（需要÷キャパ）で開始**。乖離が大きい夜間低ボリューム帯は注記 |
| 3 | 夜間の目標水準（夜間Miss率閾値） | **「できる限りゼロ」**。運用上の閾値は**0%（missed発生1件でも🟡有人Missとしてフラグ）**とし、実際のMiss率は段階色分けで深刻度を表示する。全体目標8.8%とは別基準 |
| 4 | 夜間帯定義：21-02のみ or 02-04も監視 | **21-02 + 02-04も監視**（band集計は21-02のみだがhourlyビューで02-04を補完監視） |
| 5 | シフト実績(欠勤反映)のデータソース有無 | **無し → シフト予定を実人数の代替として使用**（欠勤反映はPhase3以降） |
| 6 | 需要範囲：ライブのみ or mail/AI Video含む | **AI Video Callも含める**。※調査の結果、`v_staffing_volume_bands`/`_hourly`のdemand_vol・total_volは`channel<>'Mail'`で集計しており**既にCall+VideoCallを合算済み**（Mailのみmail_volで別出し）。追加のデータ統合作業は不要 — 現行ビューをそのまま使えばこの要件を満たす |
| 7 | 週の定義・締め | **月〜日・JST基準**（KPI Supabase側の日付基準に合わせる。SCOPEシフト運用はICTのため、日付境界で最大±時間のズレが出得る点は許容） |
| 8 | 報告媒体 | **SCOPE内投影（週次タブ）**。v1から専用タブとしてSCOPE（このアプリ）内に実装し、既存のKPI Supabase接続・在席算出ロジック（volume.js方式）を再利用する。PDF/Markdown出力は行わない |
| 9 | OP変更のShuya決裁範囲 vs 上長承認 | **Shuyaに全権委任・事後報告のみ**。レポートは意思決定の場ではなく結果共有の場と位置付ける |
| 10 | 翌週理論値の手動補正(祝日/イベント) | **週初にShuyaが入力**（レポートタブ内に手動補正欄を設ける） |

---
*根拠：KPI Supabase `teovwpuubkefbvuuxzvo`（v_staffing_volume_bands/_hourly/_missed_detail、および実体ビュー定義の直接確認）、SCOPE（Shift Management: volume.js/app.js）の時間帯バンド定義・在席算出・キャパ自動推定、5月知見（ライブMiss率≈8.8%・ワースト15/22/16時）。Fable作成 v0／§8確定はShuyaヒアリング(2026-07-04)。Notionチケット=TSK-103。*

## 9. v1実装メモ（2026-07-04）

- 実装場所：SCOPE本体に新タブ「📅 週次稼働」を追加（`weekly-staffing.js` 新規、`index.html`/`nav.js`に配線）。volume.jsと同じくクライアント側でKPI Supabaseから直接fetchし、`window.ShiftApp.staffedEmployeesInBand`で在席数を突合する方式（Firestore同期済みのシフトデータをブラウザ側でそのまま利用でき、追加のデータ移送が不要なため）。
- 対象週：データ確認時点（2026-06-24が最新取込日）の直近**完全な**Mon-Sun週＝2026-06-15〜06-21を「当週振り返り」、2026-06-22〜06-28を「翌週プラン」の例として組む。タブ自体は日付固定ではなく「直近の完全な週」を自動算出する。
- 半手動の範囲：①祝日/イベント補正欄（テキスト入力、保存はローカルのみ）②アクション台帳（前週打ち手→今週結果、手動記入）。自動化・Supabase永続化はPhase3。
